import { FeedbackItem } from "@/features/feedback";
import { HeroSection } from "@/features/hero";

function Main() {
    return (
        <main>
            <HeroSection />
            <FeedbackItem />
        </main>
    )
}

export default Main;